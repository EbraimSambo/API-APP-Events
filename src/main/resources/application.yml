server:
  port: 3001
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/events-app
    username: ebraimsambo
    password: Deezycheezy@2
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
logging:
  level:
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql: TRACE
